spring:
  application:
    name: secure

error:
  whitelabel:
    enabled: false

server:
  port: 8080
  servlet:
    context-path: /secure

app:
  secret: top-secret

management:
  endpoints:
    web:
      exposure:
        include: info, health
